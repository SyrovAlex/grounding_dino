# Grounding DINO Zero-Shot Detection Pipeline

Ноутбук для **безклассовой (zero-shot)** детекции объектов с помощью [Grounding DINO](https://github.com/IDEA-Research/GroundingDINO),  
сохраняющий **сырые текстовые предсказания модели** (как есть) в `.txt` файлы и визуализируя результаты на изображениях.

> ✅ Подходит для:  
> - сбора разметки без предопределённых классов,  
> - анализа фраз и неопределённости (`logit`),  
> - формирования пулов сложных/аномальных примеров,  
> - последующей фильтрации/кластеризации фраз (например, `"person"`, `"man"`, `"woman"` → `"person"`).

## 📁 Структура проекта
```
grounding_dino/  
├── input/                 # 📂 Исходные изображения (создаётся пользователем)  
├── outputs/  
│   ├── vis/               # 📂 Визуализации с ббоксами  
│   └── labels_raw/        # 📂 .txt файлы с сырыми текстовыми предсказаниями  
├── grounding_dino.ipynb   # 🎯 Основной ноутбук  
└── README.md   
```

## 🔧 Установка

```bash
# Клонируем текущий репозиторий
git https://github.com/SyrovAlex/grounding_dino
cd grounding_dino

#Запуск ноутбука
jupyter grounding_dino.ipynb

```

## 🚀 Обработка изображений  
Исходные изображения необходимо загрузить в папку _input_. Необходимо задать текстовое описание для искомых объектов (переменная PROMPT).  
По результатам работы в папке _outputs/labels_raw_ будут созданы файлы с метками в формате yolo, только вместо класса
будет текстовое описание объекта, которое ему даст модель. Изображения с нарисованными боксами сохраняются в папке _outputs/annotations_. 

## 💾 Формат выходных данных

### Файлы меток (`.txt` в `labels_raw/`)
Каждая строка соответствует одному обнаруженному объекту и содержит: 
```
<class_name> <confidence> <x_center> <y_center> <width> <height> 
``` 

**Где:**
*   `<class_name>` — **текстовая фраза** от модели (например, `"a man in red shirt"`, `"dog"`, `"unclear object"`).
*   `<confidence>` — значение `logit` (уверенность модели в данном предсказании).
*   `<x_center> <y_center> <width> <height>` — координаты bounding box в формате **YOLO** (нормированные от 0 до 1).

**Пример содержимого файла `image_01.txt`:**
```
"person" 0.87 0.351 0.412 0.102 0.285  
"red car" 0.72 0.655 0.521 0.211 0.118  
"tree" 0.65 0.123 0.789 0.045 0.201 
```

## ⚙️ Настройка пайплайна (внутри ноутбука)

Перед запуском вы можете настроить следующие параметры в ячейках ноутбука:
*   `BOX_THRESHOLD` (по умолчанию `0.35`) — порог уверенности для отсечения слабых детекций.
*   `TEXT_THRESHOLD` (по умолчанию `0.25`) — порог для классификации текстовых описаний.
*   `PROMPT` — **текстовый запрос** для модели. Можно задавать:
    *   Единичный объект: `"cat"`.
    *   Несколько объектов: `"cat . dog . car"` (важно использовать точку с пробелами как разделитель).
    *   Сложные описания: `"a shiny bicycle . something on the ground"`.
*   `INPUT_DIR`, `ANNOTATION_DIR`, `LABELS_DIR` — пути к директориям.  
 

## 💡 Советы и примечания

*   **Качество промпта**: Результаты сильно зависят от формулировки `PROMPT`. Экспериментируйте с синонимами и описаниями.
*   **Пороги**: Если модель пропускает объекты — попробуйте снизить `BOX_THRESHOLD`. Если появляется много шума — повысьте его.
*   **Производительность**: Обработка происходит на CPU (если не настроен CUDA). Для батч-обработки больших наборов данных рекомендуется адаптировать код из ноутбука в `.py` скрипт.
*   **Постобработка**: Собранные текстовые метки можно:
    1.  **Фильтровать** по ключевым словам (`"car" in phrase`).
    2.  **Кластеризовать** с помощью эмбеддингов (например, `sentence-transformers`).
    3.  **Вручную разметить** небольшой набор уникальных фраз, а затем автоматически проецировать классы на весь датасет.
*   **Неопределённость**: Высокое значение `confidence` не всегда равно точности *текстовой* метки. Модель может быть уверена, но ошибаться в определении объекта.  

## Кластеризация с помощью эмбеддингов
Автоматическое группирование семантически близких описаний.
```
from sentence_transformers import SentenceTransformer
from sklearn.cluster import DBSCAN
import numpy as np

# Шаг 1: Получаем векторные представления всех текстовых меток
model = SentenceTransformer('all-MiniLM-L6-v2')
texts = ["a man", "person", "woman with bag", "child playing", "red car", "vehicle", "tree"]
embeddings = model.encode(texts)

# Шаг 2: Кластеризуем
clustering = DBSCAN(eps=0.3, min_samples=2).fit(embeddings)

# Шаг 3: Смотрим результаты
for text, cluster_id in zip(texts, clustering.labels_):
    print(f"'{text}' → кластер {cluster_id}")
```

Что это даёт:
```
"a man" → кластер 0
"person" → кластер 0  
"woman with bag" → кластер 0
"child playing" → кластер 0
"red car" → кластер 1
"vehicle" → кластер 1
"tree" → кластер 2  # выброс (шум)
```

Цели применения:  
Обнаружение естественных категорий: Увидеть, как модель сама "воспринимает" мир — какие описания она считает похожими.  
Сокращение ручной работы: Вместо просмотра 1000 уникальных фраз просмотреть 10-20 кластеров и дать им имена.  
Выявление проблемных зон: Кластеры с низкой плотностью или большим разбросом могут указывать на неопределённость модели.  